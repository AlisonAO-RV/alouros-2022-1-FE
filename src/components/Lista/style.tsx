import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  textA: {
    textDecoration: "none",
    color: "#212121",
  },
  espaco: {
    marginRight: "15px",
  },
}));
